import { type Config } from "tailwindcss"

const config: Config = {
  theme: {
    extend: {
      colors: {
        primary: "#17A4FC",
        secondary: "#FACE49",
      },
    },
  },
  plugins: [],
}

export default config;
